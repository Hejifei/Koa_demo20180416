<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
    <!-- <%= result %> -->
    <form method="POST" action="/api/user/edit.json">
        <input type="hidden" value="<%= result.id %>" name="id">
        <div><span>用户名</span><input type="text" value="<%= result.name %>" name="name"></div>
        <div><span>E-mail地址</span><input type="text" value="<%= result.email %>" name="email"></div>
        <div><span>密码</span><input type="password" value="<%= result.password %>" name="password"></div>
        <div><span>level</span><input type="text" value="<%= result.level %>" name="level"></div>
        <div><button type="submit">确 定</button></div>       
    </form>
    <script src="/output/dist/js/jquery.js"></script>
    <script>
        function userDelete(idget){
            $.ajax({
                url:'http://localhost:3001/api/user/Delete.json',
                type:'POST',
                data:{id:idget},
                success:function(res){
                    console.log(res)
                },
                error:function(xhr,textStatus){
                    console.log('错误')
                    console.log(xhr)
                    console.log(textStatus)
                },
            })
        }
    </script>
</body>
</html>
